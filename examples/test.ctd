fn compute(
    x: int,
    y,
    scale = 3.14
):
  result := x + y * 2

  if result > 10:
    scale = scale + 1.0
  elif result < 0:
    scale = 0.0
  else:
    scale = scale - 1.0

  while result > 0:
    result = result - 1

outer := 42
