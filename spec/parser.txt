statements -> (statement NEWLINE+)*

statement  -> expression

expression -> term ((PLUS | HYPHEN) term)*

term       -> factor ((STAR | FWD_SLASH) factor)*
            | factor

factor     -> (PLUS | MINUS) factor
            | atom

atom       -> INTEGER
            | FLOAT
            | IDENTIFIER
            | LPAREN expression RPAREN

