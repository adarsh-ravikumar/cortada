# Functions

Functions in Cortada are defined using the following syntax:

`fn function_name (param1: Type, param2: Type, ...):
  statements`

A function with no paramters is explicitly defined as follows:

`fn function_name :
  statements`
